# 2026-03-31

## 活动记录

### Miniconda 安装

- 在 /root 下安装了 Miniconda3
- 版本: conda 26.1.1, Python 3.13.12
- 已执行 `conda init bash`，配置写入 ~/.bashrc
- 安装脚本已清理

### Git SSH 代理配置

- 配置 `~/.ssh/config`，让 `git@github.com` 走本地代理
- 安装了 netcat-openbsd
- 使用 HTTP CONNECT 方式（`nc -X connect`）
- 测试通过，用户名 `xucj98`

### 任务执行策略讨论

- 核心原则：耗时任务派 subagent，不要让用户干等
- Feishu + subagent 的正确使用模式：沙盒 + 超时 + 汇报 → 用户决策
- 用户角色：监督结果，不是监督过程

### VLM 机器人数据质量评估调研

- 创建调研规范文档：`memory/vlm-quality-eval-survey-spec.md`
- 已写入飞书：https://nics-efc.feishu.cn/docx/Qy6odDhk6ocACzxRwZycPXu7ndd
- Benchmark 设计要求：数据来源、标注方式、具体样例、指标定义

### Memory 文件结构优化

- 学习了 Skills 加载机制（session 级别，不会主动卸载）
- 重构：MEMORY.md 放入口，memory/xxx.md 放细节
- 原则：只放长期会用到的东西，一次性任务不写入口
- 创建 `memory/feishu.md`：飞书文档操作详细流程
